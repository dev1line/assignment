#!/usr/bin/env bash
# Migrations run automatically on every ECS task start (see scripts/docker-entrypoint.sh).
# Use this only if you need a one-off migrate without redeploying (e.g. custom image :migrate).

exit 0
